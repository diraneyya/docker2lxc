function entrypoint() {
    SHELL=$(ps -o comm= $$)
    SHELL=${SHELL#-} # to handle '-zsh' on Mac OS X

    if [ -p "/dev/fd/1" ] || [ -f "/dev/fd/1" ]; then
        echo "non-interactive";
    else
        echo "interative"
    fi
}

entrypoint $*